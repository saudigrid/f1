# البنية اللغوية — والطريق إلى الإنجليزية

الموقع عربي اليوم. هذا الملف يشرح ما **صار جاهزاً** للإنجليزية، وما **بقي**،
وكم يكلّف — بلا تهويل ولا تهوين.

## الطبقات الثلاث

| الطبقة | الحالة | لماذا |
|---|---|---|
| **البيانات** | ✅ جاهزة | كل نوع يحمل `nameEn` بجانب العربي. الإنجليزي يأتي من الواجهة مجاناً، فلا يحتاج قاموساً أصلاً |
| **الآلية** | ✅ جاهزة | `src/lib/i18n` — لغة واحدة مصدرها `getLocale()`، وقاموس نصوص، واتجاه ووسم `lang` وتنسيق تاريخ |
| **نصوص الواجهة** | ⏳ جزئياً | الهيكل العام وصفحات السائقين انتقلت. الباقي مكتوب عربياً داخل المكوّنات |

قِس الباقي في أي وقت:

```bash
npm run i18n:audit          # ملخّص لكل ملف
npm run i18n:audit -- --list  # كل سطر على حدة
```

## كيف تُكتب النصوص

```tsx
import { t } from '@/lib/i18n';

<h2>{t('drivers.champions')}</h2>
```

المفتاح **يجب** أن يكون موجوداً في `src/lib/i18n/strings.ts`، وإلا فهو خطأ
ترجمة برمجية لا نصّ ناقص وقت التشغيل. والإنجليزية ملزَمة بتغطية كل مفتاح عربي
بحكم النوع `Record<StringKey, string>` — فلا يتسلّل نصّ عربي إلى صفحة إنجليزية.

## ما لا يدخل القاموس

- **أسماء الأعلام** (سائقون، حلبات، مدن، دول) — لها قواميس في `src/data/i18n`
  تملؤها سكربتات التعريب، والإنجليزي فيها يأتي من الواجهة.
- **الصفحات الثابتة الطويلة** (الخصوصية، السياسة التحريرية، عن الموقع) —
  هذه تُكتب لكل لغة على حدة. ترجمة فقرة قانونية مفتاحاً مفتاحاً تُنتج نصّاً
  ركيكاً في اللغتين.
- **محتوى الحقب** (`src/data/eras.ts`) — نصّ تحريري، يُكتب لا يُترجَم.

## يوم تُضاف الإنجليزية

نقطة التبديل **واحدة**: `getLocale()` في `src/lib/i18n/config.ts`. اليوم تعيد
`'ar'` ثابتاً لأن العربية هي المعروض فعلاً. الخطوات حينها:

1. أضف مقطع لغة للمسارات: `src/app/[locale]/…`
2. اجعل `getLocale()` تقرأ ذلك المقطع بدل إعادة الثابت
3. أضف `alternates.languages` في `metadata` لكل صفحة (إشارة `hreflang` لمحركات البحث)
4. أضف زرّ تبديل اللغة بجانب زرّ الثيم

الاتجاه (`dir`) ووسم `lang` وتنسيق التاريخ ولغة OpenGraph كلها مشتقّة من
`getLocale()` أصلاً، فلا تحتاج لمسها.

## ملاحظة على الأرقام

التواريخ تُنسَّق بأرقام لاتينية حتى في العربية (`numberingSystem: 'latn'`).
هذا مقصود: قارئ نتائج رياضية اعتاد اللاتينية في كل مصدر آخر، والأرقام الهندية
تكسر محاذاة الجداول.
