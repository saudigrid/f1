# جدولة الأتمتة

الجدول مضبوط على **مرتين يومياً** (06:00 و18:00 UTC). المصدر يتغيّر حسب
منصّة الاستضافة — راجع «من يستدعي المسار» أدناه — لكن الرقمين (الجدول
وحصّة النموذج) يبقيان مرتبطين مهما كانت المنصّة.

## لماذا مرتين لا كل نصف ساعة

الحساب المجاني على Gemini يعطي **20 طلباً يومياً** لـ `gemini-3.8-flash`.
وكل خبر يكلّف نداءين (صياغة + تدقيق):

```
4 أخبار × 2 نداء × 2 تشغيلة  =  16 نداءً يومياً
```

داخل حصة الـ20 بهامش. وإن نفدت لأي سبب، التوقف المبكر يتكفّل: تتوقف التشغيلة
فوراً وتحفظ ما أنجزته بدل أن تحرق محاولات فاشلة.

جدول كل نصف ساعة (48 تشغيلة) كان سيستهلك الحصة في أول عشر دقائق من اليوم،
ثم يفشل 47 مرة.

## من يستدعي المسار

`/api/cron/ingest` لا يعرف من ناداه — يتحقّق فقط من ترويسة
`Authorization: Bearer $CRON_SECRET`. فالجدولة نفسها خارج كود الموقع تماماً،
وتختلف باختلاف المنصّة:

### على Vercel

`vercel.json` يحمل جدول الكرون المدمج، ويلتقطه Vercel تلقائياً عند الربط —
لا إعداد إضافي.

### على أي منصّة أخرى (نتليفاي، خادم خاص، …)

`vercel.json` لا يعني شيئاً خارج Vercel — يبقى في المستودع بلا ضرر لمن
يرجع إليها لاحقاً، لكنه لا يُشغّل شيئاً. الجدولة تنتقل إلى خدمة كرون خارجية
تستدعي الرابط بنفس الترويسة:

**[cron-job.org](https://cron-job.org)** (مجاني، بلا تسجيل معقّد):

1. أنشئ Cronjob جديدة
2. الرابط: `https://<موقعك>/api/cron/ingest`
3. الجدول: `0 6,18 * * *` (نفس توقيت Vercel — UTC)
4. **Headers** → أضف `Authorization: Bearer <قيمة CRON_SECRET>`
5. Request method: `GET`

أي خدمة كرون أخرى (Netlify Scheduled Functions، خادم خاص بـ crontab،
GitHub Actions المجدولة) تعمل بالطريقة نفسها: طلب GET بترويسة واحدة.

## بعد ترقية الخطة

عدّل الرقمين معاً — أحدهما بلا الآخر يخلق نفس التعارض:

| الإعداد | من | إلى |
|---|---|---|
| جدول الكرون (أيّاً كان مصدره) | `0 6,18 * * *` | `*/30 * * * *` مثلاً |
| `src/lib/config/pipeline.ts` → `maxArticlesPerRun` | `4` | `12` مثلاً |

⚠ احسب الناتج قبل النشر: كل نصف ساعة × 12 خبراً = **576 خبراً يومياً**.
هذا رقم تحريري قبل أن يكون رقم فاتورة.
